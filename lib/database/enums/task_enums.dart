enum RepeatType {
  none,
  daily,
  weekly,
  monthly,
  yearly,
  custom,
}

enum TaskPriority {
  critical,
  high,
  medium,
  low,
  optional,
}

enum TaskStatus {
  pending,
  completed,
  skipped,
  missed,
}